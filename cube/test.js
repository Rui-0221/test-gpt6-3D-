const assert=require('node:assert/strict'),{Cube,moves}=require('./engine.js');let checks=0;
for(const [name,[a,l,d]] of Object.entries(moves)){const c=new Cube(),initial=c.signature();c.turn(a,l,d);assert.equal(c.solved(),false,name+' changes state');c.turn(a,l,-d);assert.equal(c.signature(),initial,name+' inverse');for(let i=0;i<4;i++)c.turn(a,l,d);assert.equal(c.signature(),initial,name+' four turns');checks+=3}
const c=new Cube(),initial=c.signature(),sequence=[];let seed=412;for(let i=0;i<1000;i++){seed=(seed*1664525+1013904223)>>>0;const m=Object.values(moves)[seed%9];sequence.push(m);c.turn(...m)}assert.equal(new Set(c.cubies.map(c=>c.p.join(','))).size,27);assert.equal(c.cubies.flatMap(c=>c.stickers).length,54);assert.equal(c.solved(),false);for(const [a,l,d] of sequence.reverse())c.turn(a,l,-d);assert.equal(c.signature(),initial);checks+=4;console.log(checks+' checks passed: all face/middle turns, inverses, 1000-move reversal, cubie and sticker invariants');
